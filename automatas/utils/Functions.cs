using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection.Metadata.Ecma335;
using System.Text;
using System.Threading.Tasks;

namespace automatas.utils
{
    internal class Functions
    {
        //public void ValidateString(string state,char letter,int position,string text)
        //{

        //    int index = Array.IndexOf(alphabet, text[i]);
        //    char letter = text[i];
        //    string[] transition = (string[])transitionsTable[letter];
        //    string state = transition[index];
        //}
    }
}
